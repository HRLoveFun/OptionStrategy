# Environment variable examples for Market Observation Dashboard
# Copy this file to .env and adjust values as needed.

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change_this_secret_key
PORT=5000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cache configuration (if Flask-Caching used later)
CACHE_TYPE=SimpleCache
CACHE_DEFAULT_TIMEOUT=300

# External API toggles
ENABLE_OPTION_ANALYSIS=true

# Default analysis parameters
DEFAULT_FREQUENCY=W
DEFAULT_RISK_THRESHOLD=90

# Placeholder for future database or auth integration
# DATABASE_URL=sqlite:///app.db
# AUTH_PROVIDER=none
